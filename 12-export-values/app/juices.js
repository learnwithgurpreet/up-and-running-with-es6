export const types = ['Apple', 'Pineapple', 'Guawa', 'Raspberry'];
export const method = () => {
  return 'Returning method';
};

// You can export default methods also just adding default keyword. However, it will not export module twice.
export default types;